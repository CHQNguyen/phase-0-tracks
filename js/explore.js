//function to reverse a word
function reverse(value) {
	if (1 == 1)
  //a for loops that takes the total length of a word
  for (var i = value.length - 1; i >= 0; i--) {
	//command to print letters by index
	console.log(value[i]);
  }
}

reverse("Hello");